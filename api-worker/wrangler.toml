name = "blog-full-worker"
main = "src/index.ts"
compatibility_date = "2024-07-01"
# 开启Node兼容，解决svg-captcha找不到path/assert
compatibility_flags = ["nodejs_compat"]

# 唯一Hyperdrive：只读 zmzaxgIndex
[[hyperdrive]]
binding = "HYPERDRIVE"
id = "7d7f1fb08aaa4703a351120a6d6ab8cb"

# 空变量块，无R2
[vars]

# 定时任务正确格式
triggers.crons = ["0 * * * *"]
