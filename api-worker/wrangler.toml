name = "blog-full-worker"
main = "src/index.ts"
compatibility_date = "2024-07-01"

# 唯一Hyperdrive：只读 zmzaxgIndex
[[hyperdrive]]
binding = "HYPERDRIVE"
id = "7d7f1fb08aaa4703a351120a6d6ab8cb"

# 无任何环境变量、无R2、无secrets配置块
[vars]

# 定时任务正确格式（数组，无嵌套）
triggers.crons = ["0 * * * *"]
